# HTML-parsing dependency for the wowhead ingest normalizer (f11). NOT loaded by
# init.sh and NOT in requirements.txt — isolated here so the default test suite
# stays parser-free (it runs network-free via FakeFetcher and HTML fixtures).
# The normalizer imports selectolax lazily and raises ScrapeError if it is
# missing, telling you to install this file. (R24, R25, R31)
#
# Install this only when you want to run the real ingest (python -m
# wowrag.ingest.wowhead) or the normalizer's parser-backed tests:
#     pip install -r requirements-scrape.txt
#
# httpx (the HTTP client for HttpxFetcher) is already pinned in requirements.txt
# since f9, so it is not re-declared here.
selectolax==0.4.10
