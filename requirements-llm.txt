# LLM HTTP client for OllamaLLM. NOT loaded by init.sh.
# Since f9, httpx is pinned in requirements.txt (the default test suite needs it
# for fastapi.testclient.TestClient), so it is no longer declared here to avoid
# duplication. OllamaLLM uses a lazy import; the base test suite runs
# network-free via FakeLLMProvider, so no extra LLM dependency is required.
# Install requirements-llm.txt only when you want to run OllamaLLM or its
# @integration tests against a live local Ollama server.
